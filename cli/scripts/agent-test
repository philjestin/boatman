#!/usr/bin/env bash
# agent-test — Run tests
# Generated by repo-harness. Edit as needed.
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
REPO_ROOT="$(cd "$SCRIPT_DIR/.." && pwd)"
cd "$REPO_ROOT"

echo "[agent-test] Running: go test ./..."
exec go test ./... "$@"
