#!/usr/bin/env bash
# agent-lint — Run linter
# Generated by repo-harness. Edit as needed.
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
REPO_ROOT="$(cd "$SCRIPT_DIR/.." && pwd)"
cd "$REPO_ROOT"

echo "[agent-lint] Running: echo 'No Run linter command configured. Update this script or AGENTS.md.' && exit 1"
exec echo 'No Run linter command configured. Update this script or AGENTS.md.' && exit 1 "$@"
