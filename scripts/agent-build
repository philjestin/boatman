#!/usr/bin/env bash
# agent-build — Run build
# Generated by repo-harness. Edit as needed.
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
REPO_ROOT="$(cd "$SCRIPT_DIR/.." && pwd)"
cd "$REPO_ROOT"

echo "[agent-build] Running: echo 'No Run build command configured. Update this script or AGENTS.md.' && exit 1"
exec echo 'No Run build command configured. Update this script or AGENTS.md.' && exit 1 "$@"
