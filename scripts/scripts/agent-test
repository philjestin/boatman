#!/usr/bin/env bash
# agent-test — Run tests
# Generated by repo-harness. Edit as needed.
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
REPO_ROOT="$(cd "$SCRIPT_DIR/.." && pwd)"
cd "$REPO_ROOT"

echo "[agent-test] Running: echo 'No Run tests command configured. Update this script or AGENTS.md.' && exit 1"
exec echo 'No Run tests command configured. Update this script or AGENTS.md.' && exit 1 "$@"
